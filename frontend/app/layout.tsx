import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";
import { Toaster } from "@/components/ui/toaster";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "FractionalEstate | Real Estate Tokenization Platform",
  description: "Invest in premium real estate with as little as $100. Fractional ownership made simple through blockchain technology.",
  keywords: ["real estate", "tokenization", "blockchain", "fractional ownership", "investment", "proptech"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
          <Toaster />
        </Providers>
      </body>
    </html>
  );
}
